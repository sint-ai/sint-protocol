# AAIF Evidence Items

Store one markdown file per evidence item.

Naming convention:

`YYYY-MM-DD-<type>-<slug>.md`

Create files using:

```bash
pnpm run aaif:evidence:new <type> <slug> [YYYY-MM-DD]
```
