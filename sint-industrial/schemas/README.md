# Schemas

The industrial pack reuses the canonical schemas in `docs/specs` as the
source of truth:

- `FactoryIntent`
- `CellGraph`
- `RobotActionProfile`
- `SimulationReceipt`
- `FactorySettlement`

The manifest points at the current source files so collaborators can validate
the control objects before building vendor-specific adapters.
