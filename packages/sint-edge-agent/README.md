# @pshkv/sint-edge-agent

Lightweight onboard agent runtime — millisecond latency, deterministic, small footprint. Escalates to the cloud agent only when confidence is too low.

Counterpart to `@pshkv/sint-cloud-agent` (full-featured, MCP-enabled, slower).
