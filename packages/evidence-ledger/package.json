{
  "name": "@sint/gate-evidence-ledger",
  "version": "0.1.0",
  "description": "SINT Gate — Append-only tamper-evident Evidence Ledger",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "dependencies": {
    "@sint/core": "workspace:*",
    "@noble/hashes": "^1.7.0"
  },
  "devDependencies": {
    "@sint/gate-capability-tokens": "workspace:*",
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
