# @pshkv/sint-cloud-agent

Full-featured cloud agent runtime — MCP-enabled, multi-step planning, accepts seconds-of-latency. Handles escalations from `@pshkv/sint-edge-agent` and emits `edgeUpdate` suggestions for the distillation pipeline.
