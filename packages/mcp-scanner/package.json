{
  "name": "@sint/mcp-scanner",
  "version": "0.1.0",
  "description": "CLI scanner for MCP server tool definitions — detects annotation mismatches, missing tier hints, and supply-chain risks",
  "type": "module",
  "bin": { "sint-mcp-scan": "./dist/cli.js" },
  "scripts": {
    "build": "tsc",
    "scan": "node dist/cli.js"
  },
  "dependencies": {
    "@sint/core": "workspace:*",
    "@sint/gate-policy-gateway": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
