{
  "name": "@pshkv/sim-real-matcher",
  "version": "0.1.0",
  "description": "Iteratively tunes simulation parameters to match real-world telemetry (sim-to-real loop)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "import": "./dist/index.js" } },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "vitest run"
  },
  "devDependencies": { "typescript": "^5.7.0", "vitest": "^3.0.0" },
  "files": ["dist", "README.md"],
  "license": "Apache-2.0"
}
