{
  "name": "@sint/bridge-economy",
  "version": "0.1.0",
  "description": "SINT Protocol Economy Bridge — Port/adapter integration with external economy services (balance, budget, trust, pricing)",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist *.tsbuildinfo",
    "test": "vitest run",
    "test:watch": "vitest watch"
  },
  "dependencies": {
    "@sint/core": "workspace:*",
    "@sint/gate-policy-gateway": "workspace:*",
    "@sint/gate-evidence-ledger": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
