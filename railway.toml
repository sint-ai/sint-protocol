# Gateway service config
# For badge service: set Dockerfile path to apps/badge-server/Dockerfile in Railway dashboard

[build]
dockerfilePath = "apps/gateway-server/Dockerfile"

[deploy]
healthcheckPath = "/v1/health"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
