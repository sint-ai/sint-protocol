# @sint/dashboard — ARCHIVED

This app has been superseded by the **SINT Console** Conductor module.

The `apps/dashboard` app provided: ApprovalFeed, PendingApprovals, AuditLog, PolicyPlayground.
All of these features now live in the SINT Console at the Conductor section.

The `useApprovalStream` WebSocket hook has been preserved in `apps/sint-interface/src/hooks/` for reference.

This directory is kept for historical reference only. Do not add new features here.
